{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run buildts",
    "clear-build": "lerna exec -- rm -rf ./dist",
    "create": "node ./scripts/create.js"
  },
  "devDependencies": {
    "colors": "^1.4.0",
    "lerna": "^3.19.0"
  },
  "dependencies": {
    "react": "^16.12.0"
  }
}
